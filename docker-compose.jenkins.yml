version: '3'

services:
  instaiwan-client:
    image: instaiwan/client
    build: .
    command: npm run build
    volumes:
      - ./build:/usr/src/instaiwan/build:instaiwanVolume
volumes:
  instaiwanVolume: